{{ csrfField() }}

<label for="title">Title</label>
<input type="text" name="title" value="{{ old('title', note.title || '') }}" required>

<label for="body">Note Content</label>
<textarea name="body" rows="8" cols="80" placeholder="Note content" required>{{ old('body', note.body || '') }}</textarea>

<button type="submit" name="button">Create</button>
