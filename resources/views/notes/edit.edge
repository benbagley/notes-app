@layout('layouts.default')

@section('content')
  <section class="banner py-6 bg-green-dark">
    <h2 class="text-center font-semibold text-white">Edit {{ note.title }}</h2>
  </section>

  <section class="w-full container animated fadeIn">
    <form action="/notes/{{ note.id }}?_method=PUT" method="post" class="px-8 pt-6 pb-8">
      @include('notes.form')
    </form>
  </section>
@endsection

